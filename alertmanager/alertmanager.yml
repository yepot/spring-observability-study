global:
  resolve_timeout: 1m

route:
  receiver: discord-webhook
  group_by: ['alertname']
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: discord-webhook
    webhook_configs:
      - url: http://discord-relay:5001/alert
        send_resolved: true
